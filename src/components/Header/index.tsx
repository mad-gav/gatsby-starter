import React, { FunctionComponent } from 'react';
import { StyledHeader } from './styled';

const Header: FunctionComponent = () => (
    <StyledHeader>
        <p>Welcome to the Great State Gatsby Starter</p>
    </StyledHeader>
);

export default Header;
